{
  "name": "digitearn",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "DigitEarn — digital task earning platform (Vite + Firebase, Vercel-ready, SEO optimized)",
  "scripts": {
    "dev": "node scripts/gen-task-pages.mjs && vite",
    "build": "node scripts/gen-task-pages.mjs && vite build",
    "preview": "vite preview",
    "seed": "node scripts/seed.mjs",
    "build:admin-app": "node scripts/build-admin-app.mjs",
    "test": "node --import ./tests/register.mjs tests/run.mjs && node --import ./tests/register.mjs tests/marketplace.mjs && node --import ./tests/register.mjs tests/bugfixes.mjs && node tests/web-and-apk.mjs"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.5.2",
    "firebase": "^10.12.2",
    "firebase-admin": "^14.3.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "engines": {
    "node": ">=22"
  }
}
